{
  "name": "on-signup-complete",
  "version": "1.0.0",
  "main": "local_test_server.js",
  "private": true,
  "scripts": {
    "start": "cross-env ENVIRONMENT=development functions-framework --target=main",
    "prettier": "prettier --write .",
    "test": "jest",
    "auth": "gcloud auth login && gcloud auth application-default login && gcloud config set project utoc-payment"
  },
  "author": "Martin Staadecker",
  "license": "MIT",
  "devDependencies": {
    "@google-cloud/functions-framework": "^1.6.0",
    "cross-env": "^7.0.2",
    "dotenv": "^8.2.0",
    "jest": "^26.1.0",
    "prettier": "^2.0.5",
    "supertest": "^4.0.2"
  },
  "dependencies": {
    "@google-cloud/secret-manager": "^3.1.0",
    "@sendgrid/mail": "^7.2.3",
    "google-spreadsheet": "^3.0.11",
    "googleapis": "^54.1.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
